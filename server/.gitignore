# חומר עזר (התיקייה שביקשת להסתיר)
Auxiliary_material/

# ספריות קוד כבדות (שמותקנות עם npm install)
node_modules/

# קבצי הגדרות רגישים (שם נשמור את הסיסמה ל-MySQL)
.env

# קבצי מערכת מיותרים
.DS_Store
Thumbs.db